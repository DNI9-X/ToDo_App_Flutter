import 'package:flutter/material.dart';
import 'dart:ui';

class Task {
  final String taskName, subTask, time;
  final Color taskColor;
  //
  Task(this.taskName, this.subTask, this.time, this.taskColor);
}
